import type { Meta } from '@storybook/react';
import HomePage from '.';

const meta = {
  title: 'Home Page',
  component: HomePage,
} satisfies Meta<typeof HomePage>;

export default meta;

export const Basic = {};